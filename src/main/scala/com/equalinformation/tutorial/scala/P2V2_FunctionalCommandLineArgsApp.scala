package com.equalinformation.tutorial.scala

/**
 * Created by bpupadhyaya on 9/3/15.
 * If a functional literal has only one statement and takes only one argument, you need not
 * explicitly name and specify the argument
 */
object P2V2_FunctionalCommandLineArgsApp extends App {
  args.foreach(println)
}
